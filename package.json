{
  "name": "meipuratchi-mern",
  "version": "1.0.0",
  "description": "Meipuratchi — Student Career Guidance Platform",
  "scripts": {
    "backend":      "node backend/server.js",
    "frontend":     "npm run dev --prefix frontend",
    "admin":        "npm run dev --prefix admin",
    "install:all":  "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin",
    "build:all":    "npm run build --prefix frontend && npm run build --prefix admin"
  }
}
